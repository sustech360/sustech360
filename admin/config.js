/* The Apps Script web app URL. This is an endpoint, not a secret: it is safe in
   the repository. Every request it accepts is authorised server-side.
   Set the beta and production deployments separately. */
window.MAG_ENDPOINT = "PASTE_YOUR_APPS_SCRIPT_EXEC_URL_HERE";
